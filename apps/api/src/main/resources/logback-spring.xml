<?xml version="1.0" encoding="UTF-8"?>
<!-- Structured JSON-ish console logging with traceId/spanId via MDC.
     Micrometer Tracing populates the MDC; the pattern echoes it so every log
     line can be correlated to a trace and to the client-visible error envelope. -->
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%X{traceId:-},%X{spanId:-}] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
