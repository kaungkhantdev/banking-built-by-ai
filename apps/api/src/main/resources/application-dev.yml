# Dev profile: verbose SQL logging, sampling on. Activate with -Dspring.profiles.active=dev
spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.bank: DEBUG
    org.springframework.security: INFO
